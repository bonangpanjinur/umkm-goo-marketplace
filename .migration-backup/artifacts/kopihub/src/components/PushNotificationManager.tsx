// Placeholder. Web Push subscription is now opt-in via NotificationSettings
// (Settings → Notifikasi). Background push delivery requires server-side
// VAPID + push delivery worker which is not yet configured.
export function PushNotificationManager() {
  return null;
}
